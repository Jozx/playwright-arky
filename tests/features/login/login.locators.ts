/**
 * Login Locators Object
 * Stores all selector strings for the Login page.
 * Centralizing locators makes maintenance easier if the UI changes.
 */
export const loginLocators = {
    EMAIL_INPUT: '#email',
    PASSWORD_INPUT: '#password',
    SUBMIT_BUTTON: 'button[type="submit"]',
};
